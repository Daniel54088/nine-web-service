machine:
 services:
    - docker
  node:
    version: v16.3.0

dependencies:
  cache_directories:
    - "node_modules"
  override:
    - npm install

test:
  override:
    - npm test
general:
  branches:
    only:
      - main